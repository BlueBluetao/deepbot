这份提示词文档（Prompt）专为 **Cursor, Windsurf, Bolt.new** 或其他 AI Coding 工具设计。它将引导 AI 从零开始构建这个复杂的机器人 DFM（可制造性设计）智能分析平台 Demo。

---

# AI Coding 开发指令：具身智能机器人 DFM 智能分析平台 Demo

## 1. 项目概况 (Project Overview)
*   **项目名称**：Jushen-Intelligent Robot DFM Workspace
*   **目标**：展示具身智能如何通过上传机器人图纸，自动实现物料提取（BOM）、加工工艺分析、装配逻辑评估（DFM）及供应链溯源。
*   **核心价值点**：从“模糊结构”到“精确设计”的交互式上传，以及基于真实 SOP（作业指导书）数据的 DFM 报告。

## 2. 技术栈要求 (Tech Stack)
*   **前端**：React.js / Next.js
*   **UI 库**：Tailwind CSS + Shadcn UI / Ant Design
*   **动效/可视化**：Framer Motion (用于扫描动画), Three.js (用于展示机器人模糊骨架热点)
*   **状态管理**：Zustand 或 React Context
*   **图标**：Lucide React

## 3. 页面布局与功能模块 (UI & Features)

### 阶段一：初始选择与骨架挂载 (Template & Skeleton)
*   **功能**：提供 [人形机器人] 和 [四足机器人] 两个模板选项。
*   **交互**：
    *   选择后，页面中央出现一个半透明、淡灰色的 3D 轮廓（或 2D 高级矢量示意图）。
    *   模型上分布有“热点”节点（如：头部、胸腔、左臂、右腿等）。
    *   鼠标悬停在节点上时，节点高亮并显示“待上传”。

### 阶段二：组件化图纸上传 (Smart Upload)
*   **交互**：点击节点（如：左臂），弹出侧边栏上传框。
*   **模拟逻辑**：用户上传文件后，显示“正在进行几何特征提取...”、“正在匹配工艺库...”。
*   **数据集成（参考文档 2 SOP）**：
    *   若点击“手臂”，上传后系统应自动解析出：**PFR52-2 关节、输出保持架、交叉滚子轴承、M3x14 螺钉**等。

### 阶段三：智能 BOM 与加工分析 (BOM & Process)
*   **功能**：在右侧展示实时生成的物料清单。
*   **数据集成（参考文档 1 BOM）**：
    *   **物料分类**：自动标记“机加工件”（侧板）、“塑胶件”（后下壳）、“标准件”（螺钉）、“电子料”（姿态传感器）。
    *   **表面工艺**：基于备注显示“黑色阳极氧化”、“白色喷漆”、“黑色半透明”。

### 阶段四：DFM 分析报告 (DFM Dashboard)
*   **功能**：点击“生成 DFM 报告”，显示多维度评估：
    1.  **标准化建议**：如检测到螺钉规格超过 5 种，提示“建议统一紧固件规格”。
    2.  **装配风险**：参考文档 2 中提到的“扭矩到位咔嚓声”，提示“关键步骤需增加扭矩传感器校验”。
    3.  **空间干涉**：针对“千兆扁平网线”的应用，提示“布线空间极窄，强制要求使用扁平线缆”。

### 阶段五：供应链协同 (Sourcing & Report)
*   **功能**：点击物料清单中的购买图标，模拟弹出天猫/淘宝链接（参考文档 1 中的参考渠道）。
*   **报告输出**：一键生成 PDF 预览，包含爆炸图、零件清单、成本预估和优化建议。

---

## 4. 详细开发指令 (Development Instructions)

### Step 1: 初始化基础架构
> "请初始化一个 Next.js 项目，使用 Tailwind CSS 和 Shadcn UI。建立一个侧边栏导航，主视图分为左侧的 [机器人结构树]、中间的 [3D 预览交互区]、右侧的 [实时分析面板]。"

### Step 2: 实现“模糊骨架”热点系统
> "在中间预览区，使用 SVG 或 Three.js 创建一个简化的人形机器人轮廓。定义热点区域：头部、胸部、双臂、腰部、双腿。点击某个热点时，右侧分析面板切换到对应的 '组件上传状态'。"

### Step 3: 编写模拟解析逻辑（核心）
> "请编写一个模拟解析函数 `onFileUpload(componentTag)`。
> - 当 componentTag === '左臂'，返回预设数据（基于提供的 BOM 文档）：零件包含 PFR52-2 关节 * 1, M3x14 螺钉 * 12, 塑胶壳件 * 2。
> - 模拟一个 3 秒的扫描进度条，并在扫描时显示动态文字：'识别到机加工法兰...'，'检测到黑色阳极氧化要求...'。"

### Step 4: 构建 DFM 报告组件
> "创建一个 DFM 报告卡片，包含以下视觉元素：
> 1. 一个圆形进度条显示 '可制造性得分：85'。
> 2. 三个警告标签（黄色）：'紧固件种类过多'、'布线空间冗余度低'、'建议增加定位销以辅助装配'。这些建议应参考提供的作业指导书文档中的装配难点。"

### Step 5: 集成供应链链接交互
> "在物料清单表格中，为‘延长线C口直弯头’和‘电池包外壳’添加链接图标。点击时，在 Modal 中模拟显示一个精简版的购买页面，包含商品图、单价和来自天猫/淘宝的模拟链接。"

---

## 5. 视觉风格指南 (Visual Style Guide)
*   **配色**：科技感深蓝 (#0F172A) 或 极简工业白 (#F8FAFC) + 品牌色（建议使用具身智能的主色调）。
*   **字体**：无衬线思源黑体。
*   **动效**：所有数据更新使用淡入淡出效果，分析过程增加脉冲呼吸灯动效。

## 6. 文档数据注入 (Data Injection)
*   **BOM 数据参考**：务必在代码的 `mockData.ts` 中包含：`i7-13620H 迷你主机`, `YIS320 姿态传感器`, `PFR86-2 关节组件`。
*   **工艺备注参考**：物料清单中的 `黑色阳极氧化` 和 `定制电池金属壳尺寸` 必须作为系统自动识别出的“属性”展示。